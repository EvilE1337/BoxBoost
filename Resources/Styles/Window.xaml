<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:BoxBoost.Resources.Styles">


    <Style TargetType="{x:Type ListViewItem}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ListBoxItem}">
                    <Border Name="Border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}">
                        <ContentPresenter Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" Margin="{TemplateBinding Padding}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!--<ControlTemplate x:Key="BoxWindowTemplate" TargetType="{x:Type TypeName=Window}">
        <Grid>
            <Border Name="PART_BORDER"
                    Margin="7"
                    BorderBrush="{TemplateBinding Property=BorderBrush}"
                    BorderThickness="{TemplateBinding Property=BorderThickness}">
                <Grid Name="PART_MainGrid"
                      Margin="7"
                      Background="{TemplateBinding Property=Background}"
                      TextOptions.TextRenderingMode="ClearType"
                      TextOptions.TextFormattingMode="Display">
                    <DockPanel>
                        <Grid Name="PART_TitleBar" DockPanel.Dock="Top" Background="WhiteSmoke"></Grid>
                        <DockPanel Height="32">
                            <Image DockPanel.Dock="Left"></Image>
                            <StackPanel DockPanel.Dock="Right">
                                <TextBlock Text="wwww"/>
                            </StackPanel>
                        </DockPanel>
                        <AdornerDecorator>
                            <ContentPresenter/>
                        </AdornerDecorator>
                    </DockPanel>
                </Grid>
            </Border>
        </Grid>
    </ControlTemplate>
    
    <Style x:Key="Window" TargetType="{x:Type TypeName=Window}"> --><!--Создаем стиль для окна--><!--
        <Setter Property="WindowStyle" Value="None"/> --><!--Устанавливаем для windowstyle = null т.е. окно без виндовских стилей--><!--
        <Setter Property="AllowsTransparency" Value="True"/> --><!--Возможность рисовать элементы с прозрачностью--><!--
        <Setter Property="ResizeMode" Value="CanResizeWithGrip"/>
        <Setter Property="Template" Value="{StaticResource ResourceKey=BoxWindowTemplate}"/>
    </Style>-->
</ResourceDictionary>